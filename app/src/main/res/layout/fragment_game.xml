<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login_background">

    <!-- Nome do jogador atual -->
    <TextView
        android:id="@+id/currentPlayerTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Vez de: Thrain"
        android:textColor="@color/DouradoEspecial"
        android:textSize="20sp"
        android:fontFamily="@font/cinzel_font_bold"
        android:gravity="center"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_margin="16dp" />

    <!-- Log de mensagens do jogo -->
    <TextView
        android:id="@+id/gameLogTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Thrain atacou o Goblin causando 12 de dano!"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:gravity="center"
        android:padding="12dp"
        android:background="@drawable/retangulo_borda_fundo_transparente"
        app:layout_constraintTop_toBottomOf="@id/currentPlayerTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_margin="8dp" />

    <!-- Botões de ação -->
    <LinearLayout
        android:id="@+id/actionButtonsContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toBottomOf="@id/gameLogTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/attackButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="50dp"
            android:layout_marginEnd="8dp"
            android:text="Atacar"
            android:textColor="@color/VermelhoEscuro"
            android:backgroundTint="@color/DouradoEspecial"
            android:fontFamily="@font/cinzel_font_bold" />

        <Button
            android:id="@+id/defendButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="50dp"
            android:layout_marginEnd="8dp"
            android:text="Defender"
            android:textColor="@color/VermelhoEscuro"
            android:backgroundTint="@color/DouradoEspecial"
            android:fontFamily="@font/cinzel_font_bold" />

        <Button
            android:id="@+id/passButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="50dp"
            android:text="Passar"
            android:textColor="@color/VermelhoEscuro"
            android:backgroundTint="@color/DouradoEspecial"
            android:fontFamily="@font/cinzel_font_bold" />
    </LinearLayout>

    <!-- Resultado da rolagem de dado -->
    <TextView
        android:id="@+id/diceResultTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🎲 18"
        android:textSize="36sp"
        android:textColor="@color/BordasDouradas"
        android:fontFamily="@font/cinzel_font_bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/actionButtonsContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
