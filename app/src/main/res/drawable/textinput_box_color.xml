<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- focado -->
    <item android:state_focused="true" android:color="@color/DouradoEspecial"/>
    <!-- hovered (passando o dedo/mouse) -->
    <item android:state_hovered="true" android:color="@color/DouradoEspecial"/>
    <!-- estado padrão -->
    <item android:color="@color/DouradoEspecial"/>
</selector>
